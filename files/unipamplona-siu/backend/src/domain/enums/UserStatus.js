// ============================================================
// ENUM: Estado del usuario en el sistema
// Universidad de Pamplona SIU
// ============================================================

const UserStatus = Object.freeze({
  ACTIVE:    'ACTIVE',
  INACTIVE:  'INACTIVE',
  SUSPENDED: 'SUSPENDED',
});

module.exports = UserStatus;
